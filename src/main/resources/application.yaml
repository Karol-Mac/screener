spring:
  application:
    name: screener

  data:
    mongodb:
      host: localhost
      port: 27017
      database: stock_screener
  shell:
    interactive:
      enabled: true
#    script:
#      enabled: true



yahoofinance:
  api:
    apiKey: ${YHFINANCE_API_KEY}
    base-url: https://yfapi.net



logging:
  level:
    com:
      example:
            stockscreener: INFO
    org:
      springframework:
                data:
                  mongodb: INFO